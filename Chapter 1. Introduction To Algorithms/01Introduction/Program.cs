namespace _01Introduction
{
    internal class Program
    {
        static void Main(string[] args)
        {
            // An algorithm is a set of instructions for accomplishing a task.
            // Chapter 1 talks about binary search and shows how an algorithm can speed up your code.
            // In one example, the number of steps needed goes from 4 billion down to 32!

            // NP-complete problems are in NP (The name "NP-complete" is short for "nondeterministic polynomial-time complete".),
            // the set of all decision problems whose solutions can be verified in polynomial time; NP may be equivalently defined as the set of decision
            // problems that can be solved in polynomial time on a non-deterministic Turing machine.
            // https://en.wikipedia.org/wiki/NP-completeness
            // https://en.wikipedia.org/wiki/List_of_NP-complete_problems

            // You’ll need to know basic algebra before starting this book. In particular,
            // take this function: f(x) = x × 2.What is f(5) ? If you answered 10, you’re set.

            Console.WriteLine("Just theory here!");
        }
    }
}